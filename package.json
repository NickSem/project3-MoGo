{
  "name": "project_final",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "nodemon index.js",
    "watchStyle": "node-sass-chokidar ./src/sass/index.scss ./public/style/style.css --watch --use-polling --polling-interval 1000",
    "watchAll": "npm run watch | npm run watchStyle"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "express": "^4.17.1"
  },
  "dependencies": {
    "node-sass-chokidar": "^1.4.0"
  }
}
